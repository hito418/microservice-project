server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA: serve index.html for any unmatched route so client-side rendering
    # handles it instead of returning 404.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
