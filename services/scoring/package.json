{
    "name": "@services/scoring",
    "version": "1.0.0",
    "private": true,
    "main": "dist/main.js",
    "files": [
        "dist"
    ],
    "scripts": {
        "build": "nest build",
        "dev": "nest build --watch",
        "start": "node dist/main.js",
        "lint": "oxlint",
        "typecheck": "tsc --noEmit",
        "test": "nest build && node --test \"dist/**/*.spec.js\""
    },
    "dependencies": {
        "@nestjs/common": "catalog:",
        "@nestjs/core": "catalog:",
        "@nestjs/microservices": "catalog:",
        "reflect-metadata": "catalog:",
        "rxjs": "catalog:"
    },
    "devDependencies": {
        "@repo/ts-config": "workspace:*",
        "@repo/oxfmt-config": "workspace:*",
        "@repo/oxlint-config": "workspace:*",
        "@types/node": "^22.0.0",
        "@nestjs/cli": "catalog:",
        "typescript": "catalog:",
        "oxlint": "catalog:",
        "oxfmt": "catalog:"
    }
}
