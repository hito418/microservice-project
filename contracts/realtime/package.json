{
    "name": "@contracts/realtime",
    "version": "1.0.0",
    "private": true,
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "files": [
        "dist"
    ],
    "scripts": {
        "build": "tsc -b",
        "dev": "tsc -b --watch",
        "typecheck": "tsc -b --noEmit"
    },
    "devDependencies": {
        "@repo/ts-config": "workspace:*",
        "@types/node": "^22.0.0",
        "typescript": "catalog:"
    }
}
