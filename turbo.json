{
    "$schema": "https://turbo.build/schema.json",
    "tasks": {
        "build": {
            "dependsOn": ["proto:gen", "^proto:gen", "^build"],
            "inputs": ["src/**", "tsconfig.json", "vite.config.ts"],
            "outputs": ["dist/**"]
        },
        "dev": {
            "persistent": true
        },
        "lint": {
            "inputs": ["src/**"]
        },
        "format": {
            "inputs": ["src/**"]
        },
        "test": {
            "dependsOn": ["proto:gen", "^proto:gen", "^build"],
            "inputs": ["src/**", "vitest.config.ts"]
        },
        "typecheck": {
            "dependsOn": ["proto:gen", "^proto:gen", "^build"],
            "inputs": ["src/**", "tsconfig.json"]
        },
        "proto:gen": {
            "inputs": ["proto/**"],
            "outputs": ["src/generated/**"]
        }
    },
    "remoteCache": {
        "enabled": false
    }
}
